# =============================================================================
# Game Boosting Platform - 生产环境配置模板
# 用法: 复制为 .env 并修改 ( cp .env.example .env && vim .env )
# =============================================================================

# -----------------------------------------------------------------------------
# 对外入口端口（反向代理 / 防火墙放行的就是这个端口）
# -----------------------------------------------------------------------------
FRONTEND_PORT=80

# -----------------------------------------------------------------------------
# 镜像版本（推荐钉住已验证的 commit SHA 版本，格式: sha-<40位commit>）
# 留空则使用 latest（跟踪最新发布，适合全新安装）
# -----------------------------------------------------------------------------
# BACKEND_IMAGE=ghcr.io/uskyu/game-boosting-platform-backend:sha-xxxxxxxxxxxx
# FRONTEND_IMAGE=ghcr.io/uskyu/game-boosting-platform-frontend:sha-xxxxxxxxxxxx

# -----------------------------------------------------------------------------
# MySQL（首次启动后修改密码需同时改库内账号，请一开始就设好）
# -----------------------------------------------------------------------------
MYSQL_ROOT_PASSWORD=change_me_root_password
MYSQL_DATABASE=game_boosting
MYSQL_USER=boosting_user
MYSQL_PASSWORD=change_me_db_password

# -----------------------------------------------------------------------------
# 后端安全配置（必须修改！SECRET_KEY 用于 JWT 签名）
# 生成方式: python -c "import secrets; print(secrets.token_urlsafe(48))"
# -----------------------------------------------------------------------------
SECRET_KEY=change_me_random_secret_key

# 首个管理员账号（仅在数据库为空时创建）
DEFAULT_ADMIN_EMAIL=admin@gameboost.com
DEFAULT_ADMIN_USERNAME=admin
DEFAULT_ADMIN_PASSWORD=change_me_admin_password

# -----------------------------------------------------------------------------
# AI 功能（不填则 AI 相关功能不可用，平台其余功能正常）
# -----------------------------------------------------------------------------
DEEPSEEK_API_KEY=

# -----------------------------------------------------------------------------
# 跨域来源（浏览器实际访问的地址，多个用英文逗号分隔）
# 例: https://boost.example.com,http://1.2.3.4:8090
# -----------------------------------------------------------------------------
BACKEND_CORS_ORIGINS=

# -----------------------------------------------------------------------------
# 可选项（一般不用改）
# -----------------------------------------------------------------------------
# ACCESS_TOKEN_EXPIRE_MINUTES=30
# REFRESH_TOKEN_EXPIRE_DAYS=7
# PUSH_ENABLED=false
